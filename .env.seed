# Environment variables for seeding test data
# Copy this file to .env and fill in the values

# Supabase URL and anon key (same as in your regular .env file)
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase service key (with more privileges, found in Project Settings > API)
# This is needed for the seed script to bypass RLS policies
SUPABASE_SERVICE_KEY=your-service-key
